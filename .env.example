# Choose ONE of the following:

# Option 1: OpenAI (direct)
OPENAI_API_KEY=your_openai_key_here

# Option 2: OpenRouter (recommended - supports multiple models)
OPENROUTER_API_KEY=your_openrouter_key_here

# Model Configuration (auto-detected based on API key)
# For OpenRouter, try: anthropic/claude-3.5-sonnet, google/gemini-pro, etc.
DEFAULT_MODEL=gpt-4
DEFAULT_TEMPERATURE=0.2
LOG_LEVEL=INFO
